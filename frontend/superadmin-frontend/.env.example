# Base URL of the DailyTaiyari backend API.
# Leave unset in local dev to use the Vite proxy (/api -> localhost:8000).
# In production set to the deployed API, e.g. https://api.dailytaiyari.in/api/v1
VITE_API_URL=
